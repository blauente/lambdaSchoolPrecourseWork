function contains(arr, item) {
  isInside = false;
  insideCount = 0;
  
  for (i = 0; i < arr.length; i++) {
    if (item === arr[i]) {
      insideCount++;
    }
  }
  
  if (insideCount > 0) {
    isInside = true;
  }
  
  return isInside;
}
